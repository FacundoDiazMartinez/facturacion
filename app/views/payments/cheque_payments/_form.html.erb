<%= render '/payments/shared_builder' %>

<%= @builder.fields_for :cheque_payment, @payment.blank? ? ChequePayment.new(payment_id: @builder.object.id) : @payment.cheque_payment do |cpf| %>

  <%= render '/payments/cheque_payments/shared_form', f: cpf %>

<% end -%>
