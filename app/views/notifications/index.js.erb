$("#notifications").html("<%= j render template: '/notifications/index.html.erb' %>");

if ($("#notifications-preload").is(":hidden")) {
	$("#notifications-preload").show("slow");
}else{
	$("#notifications-preload").hide("slow");
};
