<div class="table-responsive">
  <table class="table table-hover">
    <thead>
      <tr>
        <th>Enviar</th>
        <th>Nombre</th>
        <th>Dirección</th>
        <th>Documento</th>
      </tr>
    </thead>


    <tbody>
      <% Client.all.each do |client| %>
      <tr>
        <td><%= check_box_tag 'client_ids[]', client.id %></td>
        <td><%= client.name %></td>
        <td><%= client.address %></td>
        <td><%= client.document_number %></td>
      </tr>
      <% end %>
    </tbody>

  </table>
  <center>
    <% link_to "#{icon('fas', 'envelope ')} Enviar".html_safe, sended_advertisement_path(:client_ids => client_ids[]), method: :post, class: 'btn btn-primary' %>
  </center>

  <%# paginate Client.all %>
</div>
