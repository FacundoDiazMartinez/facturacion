<h2><%= t('.sign_in', :default => "Sign in") %></h2>

<br />
<br />

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

<div class="form-group">
      <%= f.label :email %>
      <%= f.email_field :email, class:'form-control col-sm-4' %>
</div>

<br />

<div class="form-group">
      <%= f.label :password %>
      <%= f.password_field :password, class:'form-control col-sm-4' %>
</div>

<br />


  <% if devise_mapping.rememberable? -%>
    <div class="form-group">
      <div class="col-md-4">
        <div class="row">
          <%= f.check_box :remember_me %> &nbsp; &nbsp;
          <%= f.label :remember_me %>
        </div>
      </div>
    </div>
  <% end -%>

  <div>
    <%= button_tag "Iniciar sesión".html_safe, type: 'sign_in', class: 'btn btn-primary', id: 'sign_in' %>
  </div>

<% end %>

<%= render "devise/shared/links" %>
