<% if @product_category %>
	<% if @product_category.errors.any? %>
		$("#errors").html("<%= j error_explanation(@product_category) %>");
	<% else %>
		'<%= form_for Product.new do |f| %> '
			$("#product_category_select").html("<%= j f.select :product_category_id, options_for_select(current_user.company.product_categories.all.map{|pc| [pc.name, pc.id]}, @product_category.id), {include_blank: "Sin categoría"}, {:class => 'form-control'} %>");
		'<% end %>'
		$("#product_category_modal").modal('hide');
		$('#product_category_modal').on('hidden.bs.modal', function (e) {
			$("#product_categories_table").html("<%= j render '/product_categories/product_categories_table' %>")
		});
	<% end %>
<% end %>