<%= form_for @product_category, remote: true do |f| %>
  <div class="modal-body">
    <%= error_explanation @product_category %>

    <div class="form-group row">
      <%= f.label :supplier_id, class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= f.select :supplier_id, options_for_select(current_user.company.suppliers.map{|s| [s.name, s.id]}), {include_blank: "Ninguno"}, {:class => 'form-control'} %>
        <i>Solo seleccione proveedor si esta categoria pertenece exclusivamente a el.</i>
      </div>
    </div>

    <div class="form-group row">
      <%= f.label :name, class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= f.text_field :name, :class => 'form-control', placeholder: "Nombre", required: true %>
      </div>
    </div>

    <div class="form-group row">
      <%= f.label :iva_aliquot, class: 'col-sm-2 col-form-label' %>
      <div class="col-sm-10">
        <%= f.select :iva_aliquot, options_for_select(Afip::ALIC_IVA.map{|iva, id| [id, iva]}, "05"), {}, {class: 'form-control'} %>
      </div>
    </div>

  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-dismiss="modal">Cerrar</button>
    <%= save_button %>
  </div>
<% end %>