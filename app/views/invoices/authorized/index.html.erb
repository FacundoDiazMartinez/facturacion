<h3 class="d-flex mb-2">
  <div class=" p-2">
    	Autorizados
  </div>
  <div class=" text-right">
		<button class="btn btn-primary " type="button" href="#" onclick="toggleAuthorized2()" id="toggle_authorized2" style="margin-top: 5px;"><%= icon('fas' ,'eye-slash') %> Autorizados</button>
	</div>
</h3>
<div class="card shadow">
  <div class="card-body">
    <div id="saldo_disponible" class="text-muted ml-4"></div>
    <div class="table-responsive-sm">
      <table class="table table-striped invoice-table" id="authorized" style="width: 100%;">
        <thead>
          <tr>
            <th scope="col">Nombre</th>
            <th scope="col">Dni</th>
            <th scope="col">¿Necesita Orden de Compra?</th>
          </tr>
        </thead>
        <tbody>
          <% @client.authorized_personals.each do |ap| %>
            <tr class="fields">
              <td><%= ap.first_name %></td>
              <td><%= ap.dni %></td>
              <td><%= ap.need_purchase_order? ? "Si" : "No" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
