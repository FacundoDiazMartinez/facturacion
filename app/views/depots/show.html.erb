<div class="p-4">
	<%= title_helper("#{@depot.name}") %>

	<div class="page-section">
		<div class="card">
			<div class="card-header">
				FILTROS
			</div>
			<div class="card-body">
				<div id="stock">
					<!-- STOCK ASOCIADO -->
					<%= render 'stock' %>
					<!-- STOCK ASOCIADO -->
				</div>

			</div>
		</div>
	</div>
</div>
